# AIB Protocol — Testnet Progress & Next Steps

**Date:** 2026-09-02
**Mainnet target:** open-source release + real testnet
**Current deployed network version:** v0.11.26 (main)
**Pending release:** v0.11.27 (feat/light-node-prune, not yet merged/tagged)

---

## 1. Network Status (6+ nodes)

| Node | Role | Notes |
|---|---|---|
| 212.56.43.128:51413 | seed + relay + **dist** | `-advertise-ip` + `-dist-dir /srv/aib-dist`, systemd (aib-seed) |
| 212.56.43.128:51415 | node3 **validator** (5ffbe1e9, 299 AIB) | systemd (aib-node3) |
| 154.53.40.40:51414 | **validator** (a5c3c095, 300 AIB) | SSH-managed |
| 182.61.43.222:51413 | **China accelerator + dist** | Baidu Cloud Beijing; systemd --user + linger; snapshot-restored (its own sync had dropped txs from block bodies) |
| 216.180.75.219:51413 | community sync-only | |
| 144.91.108.90:51413 | community sync-only | |
| 154.38.187.206:51413 | community sync-only (new) | |

- Chain height ~15.5k+; block time ~30s target, ~1/min actual
- On-chain release anchors: v1 @ block 11071 (v0.11.25), **v2 dual-hash @ block 11171 (v0.11.26: binary 832a5af3…, installer 4c3ad6ef…)**

## 2. Distribution System (A+B+C+D — all live)

- **A. P2P-port HTTP distribution**: hand-rolled minimal HTTP responder on the P2P listener (GET/HEAD sniffed before binary handshake). Serves install.sh, release.json, binaries. Live on 212:51413 and 182:51413.
- **B. Multi-source install.sh**: GitHub → aib.one → 182 (China) → 154 → 216 → 144; pinned SHA256 per source, auto-failover on mismatch.
- **C. `--verify` majority cross-check**: fetches /release.json from 3 nodes, requires ≥2 matching on-chain anchored hashes for both binary and installer.
- **D. On-chain anchors**: release publisher wallet constructs a 0-value data-output tx (anchor v2: binary+installer hashes). Old nodes accept it (standard tx) — zero chain fork. /release.json served dynamically from each node's own chain view.

## 3. v0.11.27 Content (on feat/light-node-prune, ALL tested E2E)

Branch commits (chronological):
1. `88fd14f` light-node pruning (`-prune-keep-blocks N`)
2. `1357119` /v1/blocks/fetch API + serve BlocksByRange to peers
3. `e75c3ee` refetch E2E — 3 root causes fixed (dispatch wiring, Hash recompute, pruned-node anchoring) + pruned-restart genesis crash fix
4. `3d11fc7` anchor replay scanned oldest 5000 blocks, not newest
5. `53126f5` sync-only peers' heights froze (pong carries height now)
6. `0ff1ba9` peer version awareness (real UserAgent on wire; outbound VERSION now actually parsed — was silently discarded)
7. `1ee8683` self-update advisory from on-chain release anchor (10min check, banner)

### Light node (pruning)
- `-prune-keep-blocks N`: keep last N block bodies; headers/UTXO/height-index never pruned
- Validators refuse to start with pruning enabled
- `/v1/block/<h>` → 410 Gone "pruned" for pruned heights (with re-fetch hint)
- Pruned node restart safe (genesis check falls back to height-index)

### History re-fetch
- P2P: GET_BLOCKS_BY_RANGE / BLOCKS_BY_RANGE_RESP (≤100/batch… actually 500 cap; missing list returned)
- API: `POST /v1/blocks/fetch {"from":N,"to":M}` → fetch from peers → prevHash chain validation → store
- E2E verified on 212: pruned node 410 → fetch 11 blocks → 200

### Peer visibility (explorer)
- HEIGHT column live (pong-carried)
- Click a NETWORK PEERS row → detail: node id, endpoint, full stake address (clickable), chain height, **user_agent version badge**
- **⚠ OUTDATED badge** when peer's version < on-chain release anchor; **OLD BUILD** when no user agent at all
- Old nodes' peers show frozen heights until they upgrade (pong omitempty backwards compatible)

### Self-update advisory (decentralized)
Each node compares NodeVersion (build-time -ldflags injection) against the latest on-chain release record every 10 min; prints upgrade banner with the one-liner install command. The chain is the version authority — no central server.

## 4. install.sh (updated, NOT yet re-anchored)

Current hash `060c5fbf…` (v0.11.26 content + improvements):
- 182 China accelerator in sources + --verify set
- `loginctl enable-linger` after install (systemd --user survives logout/boot)
- **PENDING: hash must be re-anchored on chain with the v0.11.27 release**

## 5. Known Issues / Debt

1. **Fresh sync stalls ~7049/8247**: pre-v0.11.21 blocks use old proposer/VRF rules — fresh nodes can't validate them. Workaround: snapshot restore. Needs either a hard-coded trust checkpoint or serving a pre-validated snapshot. **Blocks community growth — top priority next.**
2. Validator `user_agent` garbled in some old nodes' API view (display-layer, fixed by upgrade)
3. Two VRF proposer-selection code paths remain (refactor debt)
4. block_sync fetches 1 block/round (slow catch-up)
5. 91e5d831 (154) private key with 100 AIB permanently locked (historical)
6. 216/144/154.38 + most peers still on v0.11.26 or older → frozen heights in explorer until upgrade wave
7. ChainReorg utxo test known flaky (pre-existing, unrelated)
8. arm64 build absent (pinned hash placeholder `__ARM64__`)
9. Release process not scripted (tag→build→anchor one-shot missing)

## 6. Next Steps (proposed order)

1. **Merge + tag v0.11.27** (feat/light-node-prune + docs branch), GitHub release with SHA256SUMS
2. **Anchor v0.11.27 binary+new installer hashes on chain** (block ~current+)
3. **Upgrade wave**: 212 seed + node3, 154, 182 (182: pull new dist files too)
4. Verify network-wide: live heights everywhere, version badges correct, refetch cross-node
5. **Fix fresh-sync stall** (checkpoint or snapshot serving) — unblocks community onboarding
6. Script the release pipeline (tag → build both arches → SHA256SUMS → anchor → refresh dist dirs)
7. arm64 build for the pinned hash
8. Later: VRF refactor, faster block_sync, SPV light nodes (needs merkle roots)

## 7. Repo/Infra Map

- Main repo: /home/temple/aib-repo (branch feat/light-node-prune @ 1ee8683; worktree /tmp/wt-prune)
- Website: /home/temple/aib-website (explorer.html, install.sh; post-commit hook auto-deploys www)
- 212 dist: /srv/aib-dist (install.sh + v0.11.26-testnet/); seed data /srv/aib-seed/data
- 182: /root/.aib (unit ~/.config/systemd/user/aib-node.service, linger on, dist /root/.aib/dist)
- 154: SSH 154.53.40.40:31416 root; node runs at :8080/:51414
- Explorer: https://aib.one/explorer.html (5s refresh)
- TGH alert: curl https://tgh.9cat.win/api/send (key ~/aib-outreach/.tgh)
- TRASH policy: old binaries → /root/TRASH (never deleted)
